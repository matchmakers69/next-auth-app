import { ExpenseInfoFormProps } from "./defs";

const ExpenseInfoForm = ({ title }: ExpenseInfoFormProps) => {
  return (
    <div>
      <h4>{title}</h4>
    </div>
  );
};

export default ExpenseInfoForm;
